import React from 'react';
import { Spinner } from 'reactstrap';

function Loading() {
    return (
        <div>
            <Spinner>Loading...</Spinner>
        </div>
    );
}

export default Loading;
